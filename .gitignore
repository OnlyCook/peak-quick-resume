# Build output
bin/
obj/
*.user

# Release artifacts (regenerated by packaging/build-release.sh)
dist/

# All zip archives — release outputs and third-party dependency packs (safety:
# don't accidentally commit binaries / redistribute dependency mods).
*.zip

# Decompilation / research scratch (large, machine-specific, not for VCS)
scratch/

# Local machine path overrides
src/**/Directory.Build.props.local
*.local.props

# OS
.DS_Store
Thumbs.db
