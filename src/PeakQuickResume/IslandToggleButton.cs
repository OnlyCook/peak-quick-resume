// Removed (session 15, 2026-07-10): the boarding-pass island-toggle button (Phase 7) is
// no longer wanted - the maintainer's decision is that Quick Resume shouldn't show any
// boarding-pass information/controls at all: a normal boarding-pass run always starts
// today's map, and a saved run is always loaded explicitly through the F7 menu (which
// already shows/restores the exact saved level and biomes), so there is nothing left for
// a boarding-pass-side toggle to usefully do. See ROADMAP.md Phase 7/Phase 8 M8 for the
// removal writeup.
//
// This file is intentionally left as an empty placeholder rather than deleted directly -
// delete it yourself with: rm src/PeakQuickResume/IslandToggleButton.cs
